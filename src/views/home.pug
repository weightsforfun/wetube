extends base.pug
include mixin/video

block content  
    div.video_grid
        each video in videos 
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
        else 
            h4 sorry there is no video  