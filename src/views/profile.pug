extends base
include ./mixin/video

block content 
    div.profile
        if isHeroku
            img(src=user.avatarUrl)
        else 
            img(src=loggedInUser.avatarUrl)
        span=user.username
    div.video_grid
        each video in user.videos 
            +video(video)
        else 
            li sorry nothing found 